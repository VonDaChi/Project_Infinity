# Force CRLF line endings for Windows batch files so they keep working
# after being checked out on systems with core.autocrlf=input or LF defaults.
*.bat text eol=crlf

# Keep Python source as LF for cross-platform consistency.
*.py text eol=lf
